/** Accepted image MIME types for activity uploads */
export const ACCEPTED_IMAGE_TYPES = ['image/jpeg', 'image/png', 'image/webp', 'image/gif'];

/** Max file size for cover image (5 MB) */
export const MAX_COVER_SIZE = 5 * 1024 * 1024;

/** Max dimension (width or height) before client-side compression */
export const MAX_IMAGE_DIM = 1920;
