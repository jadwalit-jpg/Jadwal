{
  "family": "jadwal-cleanup-e2e",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],

  "cpu": "256",
  "memory": "512",

  "executionRoleArn": "arn:aws:iam::<AWS_ACCOUNT_ID>:role/jadwal-ecs-task-execution",
  "taskRoleArn": "arn:aws:iam::<AWS_ACCOUNT_ID>:role/jadwal-ecs-task-role",

  "containerDefinitions": [
    {
      "name": "cleanup-e2e",
      "image": "IMAGE_TAG_PLACEHOLDER",
      "essential": true,
      "readonlyRootFilesystem": false,

      "command": ["node", "dist/prisma/cleanup-e2e.js"],

      "environment": [
        { "name": "NODE_ENV", "value": "production" }
      ],

      "secrets": [
        {
          "name": "DATABASE_URL",
          "valueFrom": "arn:aws:ssm:<AWS_REGION>:<AWS_ACCOUNT_ID>:parameter/jadwal/prod/DATABASE_URL"
        }
      ],

      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/jadwal-cleanup-e2e",
          "awslogs-region": "<AWS_REGION>",
          "awslogs-stream-prefix": "cleanup-e2e",
          "awslogs-create-group": "true"
        }
      }
    }
  ]
}
